[package]
name = "trading-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "trading_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"

[profile.release]
lto = true
codegen-units = 1
